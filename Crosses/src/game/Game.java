package game;

public class Game {
	public static void main (String[] args) {
		//Board board = new Board();
		Menu menu = new Menu();
		/* add start menu
		 * fix exitdialog
		 * implement perfect ai
		 * implement customizations in color 
		 * make the screen resizable and dynamically adjust button size and font
		 * replace Board.java layout with GridBagLayout() && FIX gridbaglayout in menu lmao
		 * indicate when the screen size are locked and the game difficulty is locked (THEY ARE PROGRAMMATICALLY, you could make it say [LOCKED])
		 * resolution slider not working
		*/
	}
}
